Kotlin is a object oriented and a introduction.functional language, introduction.functions are first class citizen in kotlin.
They work as an primitives, which can be passed around and used.

Functions default return type is Unit, equivalent to Void.

Functions allow
-Default params
-Named Params
-Parameter with unknown length

An single expression function don't need the actual function block, the compiler infers the type of
the resulting expression.